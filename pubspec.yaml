name: ai_crop_diagnosis_app
description: AI 진단 및 친환경 미생물 처방 모바일 애플리케이션

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_maps_flutter: ^2.5.0
  image_picker: ^1.0.0
  speech_to_text: ^6.1.1
  provider: ^6.0.0
  intl: ^0.19.0
  charts_flutter: ^0.12.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
